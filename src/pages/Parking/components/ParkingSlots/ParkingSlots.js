import * as React from 'react';

import slots from 'assets/images/parking/slots.svg';
import car from 'assets/images/parking/car.svg';

import './ParkingSlots.scss';

const ParkingSlots = ({ split }) => {
	return <div className='parking-slots'>
	
	</div>
}

export default React.memo(ParkingSlots);