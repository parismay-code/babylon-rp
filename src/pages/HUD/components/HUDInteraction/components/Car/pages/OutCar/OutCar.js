import * as React from 'react';

import './OutCar.scss';

const OutCar = ({
	setCurrentPage,
	noVisualOption,
	setNoVisualOption,
	currentOption,
	setCurrentOption,
	nextOption,
	setNextOption,
	target,
}) => {
	return <div className="hud-interactions-car-out">
	
	</div>;
};

export default React.memo(OutCar);