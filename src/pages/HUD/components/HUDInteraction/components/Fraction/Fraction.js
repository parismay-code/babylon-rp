import * as React from 'react';

import './Fraction.scss';

const Fraction = ({ setCurrentPage }) => {
    return <div className='hud-interaction-fraction'>
        123
    </div>
}

export default React.memo(Fraction);